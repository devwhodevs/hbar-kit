---
"@hbar-kit/core": patch
"@hbar-kit/mirror": patch
"@hbar-kit/payments": patch
---

Release via npm OIDC trusted publishing (no long-lived token). First release on the tokenless CI flow; provenance remains automatic. No code or API changes.
